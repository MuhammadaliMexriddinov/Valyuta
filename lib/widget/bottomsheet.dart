import 'package:flutter/material.dart';

class Gome extends StatefulWidget {
  const Gome({Key? key}) : super(key: key);

  @override
  State<Gome> createState() => _GomeState();
}

class _GomeState extends State<Gome> {
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
